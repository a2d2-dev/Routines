---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: githubtriggers.routines.a2d2.dev
spec:
  group: routines.a2d2.dev
  names:
    kind: GitHubTrigger
    listKind: GitHubTriggerList
    plural: githubtriggers
    singular: githubtrigger
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.installationRef.name
      name: Installation
      type: string
    - jsonPath: .spec.events
      name: Events
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GitHubTrigger fires linked Routines in response to GitHub App
          webhook events.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GitHubTriggerSpec defines the desired state of GitHubTrigger.
            properties:
              events:
                description: Events is the list of GitHub event types to subscribe
                  to (e.g. push, pull_request, issues).
                items:
                  type: string
                minItems: 1
                type: array
              filter:
                description: |-
                  Filter is a CEL expression evaluated against the event payload.
                  Only events where the filter evaluates to true will fire the trigger.
                type: string
              installationRef:
                description: InstallationRef references the Secret containing the
                  GitHub App installation credentials.
                properties:
                  name:
                    description: Name is the name of the Secret holding the GitHub
                      App installation credentials.
                    type: string
                required:
                - name
                type: object
              repositories:
                description: |-
                  Repositories scopes the trigger to specific repositories.
                  If empty, all repositories accessible to the installation are watched.
                items:
                  description: GitHubRepositoryRef identifies a GitHub repository.
                  properties:
                    name:
                      description: Name is the repository name.
                      type: string
                    owner:
                      description: Owner is the GitHub organisation or user that owns
                        the repository.
                      type: string
                  required:
                  - name
                  - owner
                  type: object
                type: array
              routineRefs:
                description: RoutineRefs lists the Routines that this trigger fires.
                items:
                  description: RoutineRef references a Routine by name and optional
                    namespace.
                  properties:
                    name:
                      description: Name is the Routine resource name.
                      type: string
                    namespace:
                      description: |-
                        Namespace is the namespace of the Routine.
                        Defaults to the trigger's namespace if omitted.
                      type: string
                  required:
                  - name
                  type: object
                minItems: 1
                type: array
            required:
            - events
            - installationRef
            - routineRefs
            type: object
          status:
            description: GitHubTriggerStatus defines the observed state of GitHubTrigger.
            properties:
              conditions:
                description: Conditions contains the latest observations of the GitHubTrigger's
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastReceivedAt:
                description: LastReceivedAt is the timestamp of the last GitHub event
                  delivery.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
