---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: connectorbindings.routines.a2d2.dev
spec:
  group: routines.a2d2.dev
  names:
    kind: ConnectorBinding
    listKind: ConnectorBindingList
    plural: connectorbindings
    singular: connectorbinding
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.secretRef.name
      name: Secret
      type: string
    - jsonPath: .spec.scope
      name: Scope
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ConnectorBinding binds a Kubernetes Secret to a named connector so the Controller
          can inject credentials into agent pods without encoding provider-specific env var names
          into the Routine spec.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConnectorBindingSpec defines the desired state of ConnectorBinding.
            properties:
              inject:
                description: Inject defines how Secret keys are surfaced inside the
                  agent pod.
                items:
                  description: InjectRule defines how a single key from the referenced
                    Secret is injected.
                  properties:
                    as:
                      default: env
                      description: As specifies the injection mode (env or file).
                      enum:
                      - env
                      - file
                      type: string
                    envName:
                      description: EnvName is the environment variable name used when
                        As=env.
                      type: string
                    key:
                      description: Key is the key within the referenced Secret.
                      type: string
                    mountPath:
                      description: MountPath is the file path used when As=file.
                      type: string
                  required:
                  - as
                  - key
                  type: object
                minItems: 1
                type: array
              scope:
                default: readonly
                description: Scope defines the access level granted by this binding.
                enum:
                - readonly
                - readwrite
                type: string
              secretRef:
                description: SecretRef references the Kubernetes Secret that holds
                  the connector credentials.
                properties:
                  name:
                    description: Name is the Secret resource name.
                    type: string
                required:
                - name
                type: object
            required:
            - inject
            - secretRef
            type: object
          status:
            description: ConnectorBindingStatus defines the observed state of ConnectorBinding.
            properties:
              conditions:
                description: Conditions contains the latest observations of the ConnectorBinding's
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
