apiVersion: routines.a2d2.dev/v1alpha1
kind: WebhookTrigger
metadata:
  name: alertmanager-webhook
  namespace: default
  labels:
    app.kubernetes.io/name: routines
    app.kubernetes.io/managed-by: kustomize
spec:
  signatureScheme: hmac
  secretRef:
    name: alertmanager-signing-secret
  routineRefs:
    - name: alert-triage
      namespace: default
